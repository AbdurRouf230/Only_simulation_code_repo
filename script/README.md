# Setup scripts

- `install_deps.sh` — ROS 2 Humble, Gazebo Harmonic, PX4 SITL, MicroXRCEAgent, px4_msgs.
- Call it from the repo root via `bash install_and_run.sh`.

If you already have the stack:

```bash
SKIP_INSTALL=1 bash install_and_run.sh
```
